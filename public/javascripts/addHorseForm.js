/**
 * Created by Alle on 2017-05-28.
 */


// är connectad


/*
window.onload=function () {
//var button = document.querySelector(".sex");

    $('sex').click(function () {
        $(this).css('border', '1px solid red');
        // find any other button with a specific id, and change it back to white like
        $('button#red').css('border', '1px solid white');
        })


    $('#addHorseForm').submit(function(event) {
        event.preventDefault(); // Stops browser navigationg

        var dataObject;
        // build a json object, store in information to dataObject

        $.post('/addHorse', dataObject, function(res) {
            console.log("kör post till /addHorse med ");
            // on success execute this code
        });
    });

};*/
